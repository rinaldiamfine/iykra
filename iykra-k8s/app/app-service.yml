apiVersion: v1
kind: Service
metadata:
  name: iykra-app-service
spec:
  type: LoadBalancer  # Expose the app outside the cluster
  selector:
    app: iykra-app
  ports:
    - port: 3000
      targetPort: 3000  # Map to the same port exposed by the app container